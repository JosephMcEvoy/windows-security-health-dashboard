# Scan output - may contain host names, account names and event detail
out/
output/
*.local.json
SecurityHealth_*.html
SecurityScan_*.json
SecurityBaseline_*.json
SecurityHealth_SUMMARY_*.csv

# Test artifacts
tests/browser/report.html
TestResults*.xml
coverage*.xml
tests/.artifacts/

# Editors / OS
.vs/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Node (only used by the report browser test).
# tests/browser/package-lock.json IS committed, so CI can npm ci and Dependabot
# has something to bump.
node_modules/
